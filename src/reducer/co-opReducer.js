let initialState ={
    id:uuid,
    campaignCoop: true,
    dropin:true,
    offlinecoop:true,
    onlinecoop:true,
    maxNumberOfOfflineCoop:2,
    maxNumberOfOnlineCoop:2,
    offlinePlayerMax:2,
    OnlinePlayerMax:2,
    Platform: null,// look for platform ids
}

const questionCheck = (state, action) => {
    if(state === undefined)
        state = initialState
}
    switch(action.type){
        case:''
    }
